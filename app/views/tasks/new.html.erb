<div class="container-fluid" style="padding-top: 20px; padding-left: 30px">
<h1>New Task for <%= @event.name %></h1>
<%= render 'form', event: @event, task: @task %>

<%= link_to 'Back', event_tasks_path(@event), class: "btn btn-secondary text-white" %>
</div>
